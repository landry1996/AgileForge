# === TEMPLATE - Copier vers application-secret.yml et remplir les vraies valeurs ===
# NE JAMAIS COMMITER application-secret.yml

spring:
  datasource:
    password: REPLACE_WITH_REAL_DB_PASSWORD

  data:
    redis:
      password: REPLACE_WITH_REDIS_PASSWORD

  mail:
    host: smtp.example.com
    port: 587
    username: REPLACE_WITH_MAIL_USERNAME
    password: REPLACE_WITH_MAIL_PASSWORD

agileforge:
  security:
    jwt:
      secret-key: REPLACE_WITH_BASE64_ENCODED_SECRET_KEY_MIN_256_BITS

  ai:
    api-key: REPLACE_WITH_CLAUDE_API_KEY

  integrations:
    github:
      client-id: REPLACE_WITH_GITHUB_CLIENT_ID
      client-secret: REPLACE_WITH_GITHUB_CLIENT_SECRET
    gitlab:
      client-id: REPLACE_WITH_GITLAB_CLIENT_ID
      client-secret: REPLACE_WITH_GITLAB_CLIENT_SECRET
